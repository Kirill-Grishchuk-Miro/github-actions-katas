name: basic
on: push
jobs:
  hello-world:
    runs-on: ubuntu-latest
    container: gradle:6-jdk17
    steps:
      - name: clone
        uses: actions/checkout@v3
      - name: build
        run: ./ci/build-app.sh
      - name: test
        run: ./ci/unit-test-app.sh